[package]
name = "tpt-aura"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Core library for the AURA (Adaptive Universal Record Architecture) master media format."
keywords = ["media", "provenance", "c2pa", "semantic", "wasm"]
categories = ["multimedia", "encoding", "cryptography", "data-structures"]

[dependencies]
thiserror.workspace = true
sha3.workspace = true
ed25519-dalek.workspace = true
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
criterion = "0.8"

[[bench]]
name = "neural_throughput"
harness = false
